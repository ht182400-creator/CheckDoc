# 00 项目概述与路线图

> 视角：产品经理 + 架构师

## 1. 背景与问题

团队在多个项目下通过 **Markdown 笔记**（如 `.codebuddy/memory/`、各项目目录下的 `.md`，通用、不限定目录名）沉淀了大量"经验/陷阱/规范"知识。这些笔记**格式自由、分散、难以检索**：

- 想找"Python 相关的陷阱"要翻遍每个文件；
- 想横向对比"各语言规避方法"没有统一视图；
- 新成员无法快速吸收历史踩坑。

## 2. 产品目标

构建一个本地优先的 **MemoAlign（记忆对齐分析器）**：

1. **扫描**：递归定位根目录下任意目录的 `.md` 文件（通用采集器，不限定 `memory` 子目录）；
2. **对齐**：将每篇自由笔记抽取/归一化为统一字段（内容 / 语言范畴 / 类型 / 规避方法 / 严重度 / 来源 / 标签）；
3. **呈现**：以可交互表格呈现，支持 **表头点击排序 + 多列筛选 + 行下钻详情**；
4. **导出**：一键导出 CSV / Markdown 摘要，便于二次利用；
5. **扩展**：字段 Schema、抽取规则、LLM 后端均可通过配置扩展，无需改业务代码。

## 3. 非目标（边界）

- 不做多用户/云端协作（纯本地工具，数据不出本机）；
- 不内置文档编辑器（只读分析）；
- 不强制依赖 LLM（规则抽取开箱即用，LLM 为可选增强）。

## 4. 成功标准（验收）

| 维度 | 标准 |
| --- | --- |
| 可操作性 | 指定目录 → 一键扫描 → 表格出现，全流程 < 30s（百级文件） |
| 稳定性 | 单文件解析失败不影响整体，记录日志并跳过 |
| 性能 | 增量缓存，二次扫描仅处理变更文件 |
| 扩展性 | 新增字段仅在 `config.SCHEMA` 添加一行即可在 UI 生效 |
| 美观 | 现代卡片式布局、响应式、深浅色友好 |

## 5. 路线图

> **v0.1 之后，路线图与评审改进建议已统一排定优先级。**
> 详见：[08 综合评审报告 → 四、统一优先级总表](08_综合评审报告.md)

| 里程碑 | 范围 | 状态 |
| --- | --- | --- |
| **v0.1**（已交付） | 扫描 + 规则抽取 + 交互表格 + 筛选/排序/导出 + 增量缓存 | ✅ 完成 |
| **v0.2** = P0（2.3h） | Shell/PowerShell/Batch 语言覆盖 + 平台字段 + 重入保护 + `.gitignore` + Shell 测试 | ✅ 完成 |
| **v0.3** = P1（5.4h） | 全文检索、LLM 抽取 UI 化 + 批量重抽、规避增强、交叉统计、缓存瘦身 | ✅ 完成 |
| **v0.4** = P2（11.5h） | 图谱视图、LLM 质量评分、定时同步、Excel/PDF 导出、分组/时间筛选、记录编辑、代码高亮、K3 后端 | ✅ 完成 |
| **v0.1.0** = P4（第三方评审 W1–W13） | `ui_app.py` 1380→~200 行拆分（_ui_state/_ui_helpers/_ui_filters/_ui_dialogs/_ui_actions）、FileMeta 构造下沉 scanner、常量收敛 config、UI 纯逻辑单测、PDF 边界、空态禁用、verify 门禁固化 | ✅ 完成 |
| **v0.1.1** = P5（大表白屏治本） | WebSocket 256MB 缓冲 + 表格瘦身（`_flatten_one` 剔除 `_raw`/TEXT 截断）+ 详情按 id 取数 + 导出读 `_compute_visible()` + 增量扁平化去 O(N²) | ✅ 完成 |
| **v0.1.2** = P6（rowClick 崩溃修复） | `rowClick` 显式 `args=["row"]` + `_parse_row_click_id` 防御解析 | ✅ 完成 |
| **v0.1.3** = P7（IME 中文检索修复） | 全局/列检索 IME 合成安全：Quasar `QInput` 不转发 `composition`/`input` DOM 事件，改为客户端原生 `compositionstart/end` 监听桥接 `window.__memoalign_composing`，防抖到点读标志——合成中推迟、提交中文后才搜索（`src/_ui_filters.py`：`IME_COMPOSITION_JS`/`_is_composing`/`_attach_ime_safe_input`） | ✅ 完成 |

> 当前架构（拆分后的模块化结构、接口与配置）详见 [02 系统架构设计](02_系统架构设计.md) 与 [04 接口与配置说明](04_接口与配置说明.md)。

### 与原始路线图的对应

| 原始目标 | 归属 |
| --- | --- |
| M1 基础功能 | ✅ v0.1 |
| M2 LLM + 批量重抽 + 质量评分 | P1-2（UI 化）+ P2-2（评分） |
| M3 图谱 + 全文检索 + 定时同步 | P2-1（图谱）+ P1-1（全文检索）+ P2-3（同步） |
| 评审新增（Shell/平台/健壮性等） | P0 全部 + P1 部分 |
