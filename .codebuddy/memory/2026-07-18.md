# 2026-07-18 工作记录

## P2 架构整改（第三方评审 W1-W13）全量完成 → v0.1.0
- 用户说"现在就触发"，主代理执行 P2（W1-W13），遵循 Loop 协议 + 独立 code-explorer 复评。
- 核心改动：
  - **W1/W7**：`src/ui_app.py` 从 1380 行拆分为 `_ui_state`(共享态) + `_ui_helpers`(纯函数) + `_ui_filters`(筛选/统计) + `_ui_dialogs`(对话框) + `_ui_actions`(动作/处理器)，`ui_app.py` 仅留 `create_ui`（~200 行，满足 ≤500 行硬规则）。
  - **W2**：`scanner.FileMeta.from_path(path)` 工厂，下沉 project 推导，替换原 3 处手工构造。
  - **W3**：颜色/时间/阈值/上限/语言标签硬编码收归 `config.py`（TYPE/LANG/PLATFORM_COLOR_MAP、TIME_FILTER_DAYS、LANG_GENERIC/PENDING、DIR_BROWSE_MAX、CANDIDATE_DISPLAY_MAX、QUALITY_HIGH/MID_THRESHOLD）。
  - **W8**：抽 `record_within_time_range`/`_find_record_by_id` 至 `_ui_helpers`，新增 `tests/test_ui_helpers.py`（13 用例）。
  - **W9**：`test_exporters.test_pdf_huge_text_not_crash`（5 万字符 + 200 条）。
  - **W10**：docs/09 与 test_data.json 口径统一为 139。
  - **W11**：空态硬禁用导出/图谱/修正按钮（create_ui 初始 disable + run_scan 结束按数据启用）。
  - **W12**：SKILL.md §5/§6 停止条件对齐。
  - **W13**：`loop/verify.py` 新增 `ui_import` 导入冒烟 + `case_library` 静态检查（AST 抽 `ClassName.method` 须出现在 docs/09）。
- 复评中修复 2 缺陷（均为本次引入）：① `_ui_state.py` 初次写入为空 → 重建并声明全部共享态；② `create_ui` 指标循环遗漏 `metric_quality` 赋值分支（误落 else 覆盖 `metric_platforms`）→ 补 `elif ref_name=="metric_quality"`。
- 验证：`loop/verify.py` ALL PASS（编译 + ui_import + case_library + 8 套件）。
- 推送：`_gh_push.py v0.1.0`，master ec...，54 文件，https://github.com/ht182400-creator/CheckDoc
- P2 停止条件已满足，每日 09:00 自动化下次触发将自动停止。

## 用户实测发现运行期崩溃（v0.1.0 仍含此 bug）→ 已修复
- 现象：扫描出 904 条记录后，全局搜索/列筛选触发 `AttributeError: module 'src._ui_helpers' has no attribute '_flatten'`（`_ui_filters.py:29` 调用 `H._flatten`）。
- 根因：拆分时 `_ui_filters.py` 引用了原 `exporters.flatten` 的单值语义函数 `H._flatten`，但 `_ui_helpers.py` 只定义了整行版本的 `_flatten_records`，漏定义单值 `_flatten`。
- 修复：在 `_ui_helpers.py` 补 `def _flatten(v)`（列表→`"; ".join`，None→`""`，标量→`str()`，语义对齐 exporters.flatten）；新增 `tests/test_ui_helpers.py::TestFlatten`（UH14，含 test_list_joined_with_semicolon/test_none_becomes_empty/test_scalar_to_str）并登记 docs/09。
- 验证：`loop/verify.py` ALL PASS（编译 + ui_import + case_library + 8 套件）。**注意：远程 v0.1.0 仍含此 bug，需测试通过后推送新 tag（如 v0.1.1）重发。**

## 用户实测发现：扫描极慢 + 无进度提示（拆分回归）
- 现象：扫描 D:\Work_Area\AI 耗时 ~22s，且期间 UI 无任何"扫描中"进度（冻结）。日志顺序异常：先 `扫描完成: 命中37`（scanner.py:102）后 `开始扫描`（actions:488）。
- 根因 1（进度消失，拆分引入）：`run_scan` 第 483 行 `scanner.scan_memory_md(root)` 是**同步阻塞**调用（无 await），整个事件循环被卡 22s，status_label="扫描中" 设了也无法重绘。
- 根因 2（扫描慢，固有问题）：`_iter_memory_dirs` 用 `os.walk(root)` 递归遍历整个大根目录，含 node_modules/.git 等巨型目录，极慢。
- 修复：
  - `_ui_actions.run_scan` 用 `await asyncio.to_thread(scanner.scan_memory_md, root)` 包装阻塞扫描，事件循环得以刷新，进度提示可见（且扫描前设 `扫描中 0/{total}`）。
  - `config.SKIP_DIR_NAMES` 新增常量（.git/node_modules/.venv/__pycache__/dist/build 等），`_iter_memory_dirs` 在 os.walk 中 `dirnames[:]` 裁剪这些目录，扫描大幅加速。
  - 新增 `tests/test_scanner.py`（2 用例，验证跳过巨型目录 + 防御性断言 SKIP 集合），登记 docs/09 §13.7（SC01/SC02）、verify.py 注册（9 套件）。
  - ⚠️ 注意：`tests/test_scanner.py` 首次 `write_to_file` 报告成功但**落盘为空**（与之前 _ui_state.py 写入为空的诡异现象一致），重新写入后正常。工作区疑似偶发写空 bug，凡关键文件写后须立即 read 验证。
- 验证：`loop/verify.py` ALL PASS（编译 + ui_import + case_library + 9 套件）。
- 待办：远程 v0.1.0 仍含 _flatten 崩溃 + 扫描冻结两问题，需用户测试通过后推送新 tag（如 v0.1.1）。

## 用户实测发现：内容丢失（数据准确性回归）→ 已架构级修复
- 现象：扫描 D:\Work_Area\AI 命中从 37 文件/904 记录 暴跌到 8 文件/76 记录。用户要求通用性、参考业界框架做法。
- 根因：上轮为加速把 .codebuddy 塞进 SKIP_DIR_NAMES（全局同名目录都跳过），但 .codebuddy/memory 本身就是数据源（MemoAlign 只认任意 memory 子目录下的 .md），被整体跳过导致丢数据。这是按目录名全局跳过的典型反模式。
- 架构调研：ripgrep/fd 核心是 path-aware ignore（按路径级判断，数据源锚点永不丢）；Python 用 os.walk + dirnames 原地裁剪，进阶用并发顶层目录 + mtime 增量缓存。
- 修复（两层 path-aware 裁剪，通用、不硬编码具体目录名）：
  - config.SKIP_DIR_NAMES 移除 .codebuddy，只保留真正全局垃圾（任何层级绝不含量数据源）：.git/node_modules/dist/build/__pycache__/venv/.cache/.mypy_cache/.pytest_cache 等。
  - 新增 config.HIDDEN_DIR_TRIM 开关：进入以 . 开头的目录（.codebuddy/.cursor/.claude/任意自定义隐藏目录）后，只保留 memory 子目录，裁剪 skills/cache/plugins 等噪声。换任何 IDE/工具都通用，数据源绝不丢。
  - scanner._iter_memory_dirs 先全局 SKIP 裁剪，再隐藏容器裁剪。
- 测试：tests/test_scanner.py 重写为 4 用例（SC01 全局垃圾跳过 / SC02 隐藏容器 memory 命中-核心回归 / SC03 隐藏容器内噪声裁剪 / SC04 防御性断言 .codebuddy 不在 SKIP），覆盖通用场景。docs/09 §13.7 登记 SC01-SC04，计数 Scanner 2→4、Python 小计 65→67、总计 212→214。
- 验证：loop/verify.py ALL PASS（编译 + ui_import + case_library + 9 套件）。
- 待办：远程 v0.1.0 现含三问题（_flatten 崩溃 + 扫描冻结 + 数据丢失），需测试通过后推送新 tag 重发。

## 扫描架构重构（换构架，2026-07-18）：规则驱动 + 并发 + 增量
- 用户批评：过滤设计只关注 memory 子目录太单一，要求换构架 + 并发遍历（ThreadPoolExecutor 顶层子目录并行 os.walk）+ mtime 增量（store 已有 CACHE_DIR 雏形，run_scan 却每次 store.reset() 废掉）。
- 新架构 scanner.py：
  - MatchRule 可插拔发现规则（解耦单一 memory 锚点；默认 memory，预留 dir_name=None 任意目录规则），`scan(root, rules, max_workers)` 通用入口，`scan_memory_md` 保留作兼容别名。
  - ThreadPoolExecutor 对 root 顶层子目录并行 os.walk（overlap 磁盘 I/O，config.SCAN_MAX_WORKERS=8）。
  - 保留两层 path-aware 裁剪（全局垃圾跳过 + 隐藏容器只留数据源）。
  - 修复两并发 bug：① sub_dirs 排除 SKIP_DIR_NAMES（否则垃圾目录作 top 内部 walk 时裁剪已失效会命中其中 memory）；② _walk_subtree 开头检查 top 自身是否数据源目录（否则 root/memory 直接作 top 时内部走找不到 memory 子目录→漏命中，即 test_runner B06 的 0!=1）。
- 增量扫描落地：store.py 本已有 mtime 增量机制（needs_update/get_cached_records/put_all），唯一破坏点是 run_scan 无条件 store.reset()。改为 `run_scan(force_full=False)`，仅关键词热加载/编辑/清空等场景传 force_full=True 才清空全量重抽；UI 新增"强制全量"复选框（S.force_full_cb）可覆盖。默认点扫描走增量，仅重抽 mtime 变化文件。
- 测试：test_scanner 扩到 8 用例（SC01-SC08：裁剪/隐藏容器/可插拔规则/并发正确性+可重复），docs/09 §13.7 登记，Scanner 4→8、Python 小计 67→71、总计 214→218。
- 验证：loop/verify.py ALL PASS（编译+ui_import+case_library+9 套件，含 test_runner B06 修复）。
- 待办：远程 v0.1.0 的三个问题（_flatten 崩溃 + 扫描冻结 + 数据丢失/旧扫描架构）已本地全部修复，需用户测试通过后推送新 tag（如 v0.1.1）重发。

## 扫描架构再重构（解耦目录名，2026-07-18 16:xx）：默认"任意目录 .md"
- 用户二次批评：上一轮重构虽做了可插拔 MatchRule，但默认 `MatchRule()` = `dir_name=MEMORY_DIR_NAME`，等于换汤不换药——数据源仍死绑 memory 目录名。实测 `D:\Work_Area\TestOne\Progi-Projekt-Tim-3`（普通目录）下 6 个 .md 全漏扫（IN_MEMORY=0），用户怒"为什么要只认 **/memory/*.md"。
- 彻底重构 scanner.py v2：数据源发现不再绑定任何具体目录名，**默认 `MatchRule(dir_name=None)` = 扫描「任意目录的 .md」**，改目录结构也不丢数据。保留两层 path-aware 裁剪：① 全局垃圾(node_modules/.git/.cache 等)跳过；② 隐藏容器裁剪**改为无条件只留 memory**（无论规则如何，.codebuddy/.cursor 内只认 memory，裁剪 skills/cache/plugins 噪声）——之前这步依赖"规则含 None 才不裁剪"，已改为与规则无关，更通用。
- project 推导统一（_derive_project + FileMeta.from_path）：数据源目录本身是 memory 则上溯两级(projA)，否则取目录名(Progi-Projekt-Tim-3)，与 UI 一致。
- scan_memory_md 别名明确保留 memory-only 语义（特定场景/兼容旧行为）。
- 测试 test_scanner 扩到 10 用例（SC05-SC10：默认任意目录 + 核心回归"默认扫普通目录 .md 命中" + memory-only 兼容 + 并发），docs/09 §13.7 登记，Scanner 8→10、Python 小计 71→73、总计 218→220。
- 真实验证：scanner.scan(r'D:\Work_Area\TestOne') 命中 6 个 .md（原 0），全部来自 Progi-Projekt-Tim-3 含子目录 images/*，project 正确。loop/verify.py ALL PASS（9 套件）。
- 待办：v0.1.0 的 _flatten/扫描冻结/旧扫描架构三问题本地已全修复，用户测通过后推送新 tag（v0.1.1）。

## 文档同步 + 评审盲点诚实更正（2026-07-18 16:xx）
- 用户批评：① 我作为架构师犯了低级错——上一轮"可插拔 MatchRule"架构没改默认值，等于换汤不换药；② 第三方评审（docs/11）高调宣布 v0.1.0 通过，却漏掉"默认扫描仍是非通用 memory-only"这一核心盲点，等于基于错误假设放行（用户质疑"作弊"）；③ docs/ 下项目文档全写"固定目录 **/memory/*.md"，与用户"做成通用、D:\Work_Area 只是举例根"的初衷不符。
- 文档更正（docs/ 下所有"固定目录"口径）：
  - docs/README.md：目标、scanner.py 职责、默认扫描根（标注为示例）、匹配规则（递归任意目录 .md + 两层裁剪）。
  - docs/00_项目概述与路线图.md：沉淀知识改为"任意目录 Markdown 笔记"。
  - docs/04_接口与配置说明.md：DEFAULT_ROOT 标注"示例值 + 通用扫描"；scan 描述改为任意目录 .md。
  - docs/06_质量保障与测试方案.md：边界"无 memory 目录"→"无 .md 文件"；scan 默认通用语义。
  - docs/09_测试案例库.md：§13.7 说明改为"通用采集器"。
  - MEMORY.md 项目定位同步为"通用扫描任意目录 .md"。
- 评审诚实更正（docs/11 新增 §9 评审盲点更正）：不掩盖、不美化——明确指出 §8 P2 复评五个维度均未验证"默认扫描根语义是否通用"，功能正确性 PASS 在该维度不完备；记录盲点成因、评审责任、修复（本地待 v0.1.1）、防回归建议。保留原结论历史真实性。
- 门禁加固：loop/verify.py 新增 `scan_default` 静态检查（断言 `MatchRule().dir_name is None`，复用 _run 子进程），纳入 passed 条件，防再次回归固定目录。verify ALL PASS（含 scan_default）。
- 待办：v0.1.0 三问题 + 扫描通用性盲点均已本地修复并文档/门禁加固，用户测通过后推送新 tag（v0.1.1）。

## 流式扫描改造（借鉴 Windows 文件管理器，2026-07-18 16:4x）：解决"大目录卡死/记录无法显示"
- 用户反馈：改成通用任意目录后，D:\Work_Area\AI 命中 3338 文件/20331 记录，原 run_scan 是"先全发现(阻塞~2min)+再全解析+最后一次性 S.table.rows 赋值"三段式，UI 长时间无反馈且 2 万条一次性推给 NiceGUI 导致表格崩溃/白屏（"记录无法显示"）。指出的核心直觉：Windows 资源管理器是边扫边显示路径、不陷入长等待。
- 架构改动：
  - scanner.py 新增 `scan_iter(root, rules, max_workers)` 流式生成器（yield FileMeta，逐文件产出，惰性、不一次性全量发现）；`scan()` 改为 `list(scan_iter(...))` 批量封装，旧同步调用方零改动。并发 walk 每完成一个顶层子树即把其中文件逐条 yield（不囤积）。
  - _ui_actions.run_scan 改两阶段流式：阶段一在子线程驱动 scan_iter、主协程实时消费，status_label 显示"正在扫描（已发现 N）：…<父目录>/<文件名>"（模拟资源管理器路径滚动）；阶段二每解析 1 个文件就 append 记录并 `S.table.update()` 增量渲染，浏览器只渲染 62vh 可视区，规避 2 万条一次性卡死。用 threading.Thread + queue.Queue 解耦阻塞 I/O 与事件循环（之前用 asyncio.to_thread 整段阻塞，仍无中间反馈）。
  - 顶部提示语改为"将任意目录的 Markdown 笔记对齐为结构化表格：排序 / 筛选 / 下钻 / 导出"（去掉固定 **/memory/*.md 旧口径）。
- 测试：tests/test_scanner.py 新增 TestScanIter（SC11 流式集合==批量不丢数据 / SC12 惰性逐条产出 / SC13 顺序稳定），docs/09 §13.7 登记，Scanner 10→13、Python 小计 73→76、总计 220→223。
- 真实验证：_verify_stream.py 消费 scan_iter(r'D:\Work_Area\AI') → STREAM_HITS 3338、FIRST3 显示真实路径、BATCH_HITS 3338、SET_EQUAL True（流式==批量，不丢数据）。loop/verify.py ALL PASS（9 套件）。
- 待办：v0.1.0 三问题 + 扫描通用性盲点 + 流式渲染均已本地修复，用户测通过后推送新 tag（v0.1.1）。

## WebSocket buffer 治标（2026-07-18 22:2x）：解决大表 connection lost / 白屏
- 用户复测反馈：解析到 ~3000 条时白屏 + 右下角 `connection lost`，重连后到 ~7000 条又白屏十几秒。用户怀疑缓存。复盘根因：并非缓存逻辑错误，而是 **NiceGUI 底层 python-socketio 默认 `max_http_buffer_size=1MB`**（已查 nicegui/nicegui.py:52 `socketio.AsyncServer(async_mode='asgi', cors_allowed_origins='*', json=json)`，未设该参数）。当前每行经 `_flatten_records` 携带整篇 `_raw` 全量推送，单条 update 消息远超 1MB → socket 重置 → connection lost → 重连后又撞大消息 → 白屏循环。（缓存其实帮了忙：增量记录只剩 `_raw_preview` 2000 字，所以重扫比首扫轻；首扫带完整原文才是爆炸点。）
- 用户明确指令：**先扩充 buffer，表格先不要动**。故本次只做治标，不动表格瘦身/去 O(N²)。
- 改动：
  - config.py 新增 `SOCKETIO_MAX_HTTP_BUFFER_SIZE = 256 * 1024 * 1024`（256MB，注释说明是兜底、治本在表格瘦身）。
  - main.py 在 `from nicegui import ui` **之前** monkey-patch `socketio.AsyncServer.__init__`，强制注入 `max_http_buffer_size`（因 NiceGUI 在模块导入时即创建 server，patch 必须早于 import）。
- 验证：`_verify_buffer.py` 复刻 patch 后 import nicegui，读 `ng.sio.eio.max_http_buffer_size` = 268435456 == EXPECTED，`OK=True`。loop/verify.py ALL PASS（编译+ui_import+case_library+9 套件）。
- 治本待做（用户说表格先不动，后续再做）：表格侧瘦身——只推显示列、剔除每行整篇 `_raw`（`_flatten_records` 去掉 `_raw`、长文本截断预览），详情/导出按需按 `id` 从服务端 `S.records` 取完整数据；并把解析阶段"每文件全量 update"改为解析完一次性增量推送，消除 O(N²) WebSocket 洪流。

## 表格瘦身 + 详情按 id 取数（治本落地，2026-07-18 22:5x）
- 用户确认按之前方案实施治本（buffer 已先扩充）。改动：
  - config.py: 新增 `CONTENT_PREVIEW_MAX = 200`（TEXT 字段表格预览截断长度）。
  - _ui_helpers.py: 拆出 `_flatten_one(row)`（单条显示行：MULTI→字符串、TEXT 超长截断、剔除 `_raw`/`_raw_preview`），`_flatten_records` 复用；模块级预计算 `_MULTI_FIELD_KEYS`/`_TEXT_FIELD_KEYS`/`_DISPLAY_EXCLUDE_KEYS`。
  - _ui_filters.py: 提取 `_compute_visible()` 返回完整可见记录（含 _raw），`_apply_filters` 复用；导出改读它。
  - _ui_actions.py: 4 个 `_export_*` 改读 `F._compute_visible()`（完整记录含原文，不再从瘦身 rows 取，避免导出丢 content）；run_scan 解析循环改为增量 `S.table.rows.extend(_flatten_one(r) for r in file_to_show)` 每 20 文件 update 一次（去 O(N²) 全量重建）。
  - _ui_dialogs.py: `_open_detail` 改收 `rec_id`，内部 `H._find_record_by_id(rec_id)` 取完整记录；原文优先 `_raw`，缓存记录无 `_raw` 时按 `_path` 现读源文件兜底（`Path.read_text`）。新增 `from pathlib import Path`。
  - ui_app.py: `_on_row_click` 改为传 `row["id"]`（不再回传整行 `_raw`）。
- 验证：loop/verify.py ALL PASS（含新增 TestFlattenRecords 6 用例 UH15-UH20，docs/09 同步，UIHelpers 16→22 / Python 小计 76→82 / 总计 223→229）。_verify_slim 体积对比：单条 37KB→1.3KB（瘦身 29.4x），2万条旧版 723MB→显示行 24.6MB（<256MB buffer），且增量 update 单次仅几十 KB，白屏根因彻底消除。
- 结论：buffer 治标(256MB) + 瘦身治本 双重保障。详情完全不受影响（类比资源管理器：列表不含文件内容，双击才按 id 现读）。
