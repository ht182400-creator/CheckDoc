# 2026-07-17 工作记录

## MemoAlign 改造为 Loop Engineering（自我迭代循环）
- 理论依据：Anthropic《Designing loops with Fable 5》五组件+记忆层（用户称 K3/fable5 原理）
- 落地五组件：
  1. 自动化触发：`automation_update` 创建 recurring automation `memoalign`，每日 09:00 触发（ACTIVE）
  2. 工作树隔离：`loop/worktree.ps1` 一键建隔离分支
  3. 技能文件：`.codebuddy/skills/memoalign-loop/SKILL.md`（项目约定/意图债务/迭代协议）
  4. 连接器：`_gh_push.py`（GitHub REST API，因本机 git push 常失败）
  5. 子代理：`Task`(code-explorer) 独立验证
  - 记忆层：`.codebuddy/memory/BACKLOG.md` + `MEMORY.md`
- 验证代理入口：`loop/verify.py`（编译+unittest，输出 tests/loop_verify.json，GBK 控制台安全打印）
- 文档：`docs/10_Loop工程化与自我迭代.md`
- 停止条件：P2 三项完成 + verify 全绿 + 文档同步 + 已推送

## 现状（verify）
- 上午（搭建 loop）：编译 PASS；test_pipeline 3 FAIL；test_runner 9 FAIL + 32 ERROR（均因测试代码未同步多记录抽取/缓存格式重构，非业务 bug）
- 下午（第一轮迭代完成）：**loop/verify.py 全绿**（compile PASS + test_pipeline PASS + test_runner PASS，passed=true）。44 项失败全部修复。

## 第一轮迭代执行记录（手动推进，v0.0.4）
- 用户说"想立刻推进" → 按 Loop 迭代协议执行：发现→规划→执行→验证→迭代→状态。
- 根因（5 类断言过时）：① extract() 现返回 List[Dict]（多记录）② store.put 缓存改 `{"__mtime":...,"records":[...]}` ③ `_rule_extract` 重命名为 `_rule_extract_section`（4 参数）④ `_first_heading` 现返回"标题 | 正文摘要"⑤ Graph 重构为 treemap/network（旧测试期望力导向 nodes/links/symbolSize）。
- 改动文件：tests/test_pipeline.py（TC04/05/07 取 [0]）、tests/test_runner.py（extract→[0]、st.put→st.put_all、_rule_extract_section 调用、build_graph_option mode="network"、put_slim 查 records[0]）、tests/test_data.json（EU01 期望 "Hi | c"、G04 数据 3+1 同语言不同组合）。
- 独立验证：派 code-explorer 子代理审查代码（确认逻辑一致、lint 0 错误），但其无命令执行能力；主代理补跑 `python loop/verify.py` 闭环 → ALL PASS。
- 收尾：清理诊断临时文件 → git commit → `_gh_push.py` 推送 master + 打 tag v0.0.4 → 更新 BACKLOG/MEMORY。

## 连接器与推送修复（v0.0.4 收尾）
- 原 `_gh_push.py` 是旧的 v0.0.3 硬编码脚本，首次推送把磁盘文件（含 v0.0.4 改动）推成 commit bf6e36a，但元数据错标 v0.0.3、漏推 Loop 产物、且混入 `.cache/`/`MemoAlign.log`(13MB)/`.pytest_cache`。
- 重写 `_gh_push.py`：版本参数、自动遍历仓库收集文件、排除日志/缓存、推送时**主动删除**远程冗余文件、tag 幂等（move）。
- 经多次清理推送，最终远程 master=4fb6cea，tag v0.0.4 落在该 commit，仓库干净（44 文件，无日志/缓存）。
- 注意：本机 git 与远程 API 推送为两条并行历史（本地 4f2565c vs 远程 4fb6cea，内容一致），后续迭代统一走 `_gh_push.py`(API) 以保持远程为权威源。
- 本地 `v0.0.4` tag 已存在（与远程 SHA 不同但内容同），无需强制移动。

## K3 讨论澄清（用户确认"K3=月之暗面新模型"）
- 用户最初把"K3/fable5"并列为自我迭代方法论；经联网检索确认：
  - **K3 = Kimi K3**：月之暗面 2026-07-16 发布的 2.8T 开源模型（MoE、KDA 混合注意力、100万token 上下文、原生视觉、面向软件工程/深度研究，全球最大开源模型）。是"模型/引擎"，非方法论。
  - **Fable 5 / Loop Engineering**：Anthropic 方法论（五组件+记忆层）。
- 本 Loop 架构**模型无关**：当前自动化默认 IDE 内置模型；未来可切 Kimi K3 API 或本地权重，五组件不动。
- 文档落地：`docs/10_Loop工程化与自我迭代.md` 新增 §2.1（K3 关系澄清）+ §7（每日 09:00 跑完一轮后的处理：成功路径 commit/push/tag/状态回写/停止判定；失败路径不推送+最多2轮修复+转人工）。
- 用户决定：暂不实际验证（不跑 loop 第一轮），仅把讨论写入文档。

## 晚上：用户要求"不用等 9 点，现在开始自动化演进" → 连跑 3 轮 Loop 迭代
用户不想等到每日 09:00，要求立即推进自动化演进。按 Loop 迭代协议（发现→规划→执行→独立子代理验证→commit→_gh_push.py 推送→状态回写）连做 3 轮，每轮均 `loop/verify.py` 全绿 + code-explorer 子代理独立复核通过。

### 第二轮（v0.0.5）：P2-2 LLM 质量评分
- 新增 `src/quality_scorer.py`：双模式——离线启发式（字段完整度 20+20+15+20+15+10=100）+ LLM 语义评分（启用且配密钥时走 `llm_client.LLMExtractor.score()`，失败回落 heuristic）。
- `src/llm_client.py` 增 `score()`；`config.py` 增 QUALITY_SCORE_KEY/阈值/等级/EXTRA_EXPORT_FIELDS。
- `ui_app.py`：质量分列 + 彩色等级徽标 slot、质量等级筛选、批量质量评分按钮、平均质量分指标卡、扫描自动启发式评分、CSV/Excel/MD 导出含质量分。
- 新增 `tests/test_quality.py` 并纳入 `loop/verify.py` 套件（注意：write_to_file 偶发写出空文件，重试后正常）。
- commit `d16316f` → 推送 `ef39ef9` + tag v0.0.5。

### 第三轮（v0.0.6）：P2-3 定时增量同步
- 新增 `src/sync.py`：`compute_diff` 按 `_path` 主键 + `_mtime` 比较，产出 added/changed/removed；`summarize`。
- `config.py` 增 AUTOSYNC_ENABLED/INTERVALS/DEFAULT_INTERVAL。
- `ui_app.py`：增量同步按钮（快照旧 records→run_scan→diff→通知）+ 自动同步开关（`ui.timer`，5/15/30/60 分可选）+ 间隔下拉。
- 新增 `tests/test_sync.py` 纳入 verify 套件。
- commit `4dd7b09` → 推送 `464f576` + tag v0.0.6。

### 第四轮（v0.0.7）：P2-4 代码高亮（最后一项）
- `ui_app.py`：注入 highlight.js（CDN）；详情正文包入 `#detail-body` 容器，`_open_detail` 打开后用 `ui.run_javascript` 对新渲染的 `#detail-body pre code` 调 `hljs.highlightElement`；离线 `window.hljs` 守卫优雅降级。
- `config.py` 增 CODE_HIGHLIGHT_ENABLED/THEME；新增 `tests/test_highlight.py` 纳入 verify 套件（首测引入 nicegui 导入，属项目运行依赖，无副作用）。
- commit `261cc58` → 推送 `9c0a312` + tag v0.0.7。

### 闭环结果
- **P2 四项全部完成**，loop/verify.py 全绿（compile + 5 套件：pipeline/runner/quality/sync/highlight），文档同步（docs/08、BACKLOG、MEMORY 均更新）。
- 停止条件已满足 → 每日 09:00 自动化下次触发时自动停止（automation.toml 未在工作区/用户 home 找到，运行态在 SQLite；自停机制依赖其 prompt 读 BACKLOG 全绿）。
- 全局验证结论：本轮 Loop 真正"自我迭代"跑通——执行与验证分离（子代理独立复核），每轮验证通过才推送。

### 工具注意点
- **write_to_file 偶发写出空文件**（quality_scorer.py、sync.py 首次写入为空，重试后正常）。后续大文件写入建议写入后立刻 `python -c "import 模块"` 或读取确认内容非空，再跑测试。

## 深夜：用户追加三个需求 → v0.0.8（手动迭代，非每日自动化范围）
用户要求加入：① 导出 PDF ② 记录内编辑/修正低分 ③ 把 K3 接入为可选 LLM 后端。

### 改动与实现
- **PDF 导出**：新增 `src/exporters.py`（纯函数 `build_csv/build_xlsx/build_md/build_pdf` + `flatten`），中文走 reportlab 内置 CID 字体 `UnicodeCIDFont('STSong-Light')`（无需本地 TTF）。`ui_app.py` 的 `_export_csv/xlsx/md` 重构为调用 exporters 的薄包装并新增 `_export_pdf`；`requirements.txt` 加 `reportlab`。
- **记录内编辑/修正低分**：新增 `src/record_edit.py`（`merge_edit`/`is_low_score`/`next_low_score` 纯逻辑）。`ui_app.py` 新增 `_open_edit`（按 Schema 生成 textarea/select 编辑控件，保存写回记录+缓存 `store.put_all` 并重算 `quality_score`）+ `_correct_low_scores`（按当前可见低分队列逐条编辑，"保存并下一条"推进）。
- **K3 可选后端**：`config.py` 新增 `LLM_PRESETS`（kimi_k3: base_url=https://api.moonshot.ai/v1, model=kimi-k3）+ `LLM_DEFAULT_PROVIDER`；`ui_app.py` LLM 设置区新增"后端"下拉 `_llm_provider_select`，`_on_provider_change` 切换时自动填充 base_url/model 并复用既有 OpenAI 兼容 `LLMExtractor`。经联网确认 Kimi API 兼容 OpenAI 格式。
- **测试**：新增 `tests/test_exporters.py`（flatten/CSV 的 BOM/MD 分组/PDF 的 %PDF 头+STSong+体积/XLSX 的 PK 头，无依赖时 skip）、`tests/test_record_edit.py`（merge_edit 更新/保留内部字段/防注入、is_low_score 阈值、next_low_score）；均纳入 `loop/verify.py`（现 7 套件）。
- **验证**：`loop/verify.py` 全绿（compile + 7 套件）。独立 code-explorer 子代理审查通过（无阻断项；建议补充 UI 层函数级单测与空行边界——非阻断）。

### 收尾
- 提交 + `_gh_push.py` 推送 master + tag v0.0.8。
- 状态回写：BACKLOG（当前版本 v0.0.8 + P3 三项 ✅）、MEMORY（P3 路线）、docs/08（P3 三项 ✅ 已实现）、docs/10（§2.1 注记 K3 已接入为可选后端）。
- 坑：PDF 中文在字节流中以 CID 编码存储，不能 `assertIn(b"中文")` 原始 UTF-8 字节——改为校验 `%PDF`/`%%EOF`/`STSong`/体积。bytes 字面量不能含非 ASCII，需 `.encode("utf-8")`。
