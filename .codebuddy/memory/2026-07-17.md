# 2026-07-17 工作记录

## MemoAlign 改造为 Loop Engineering（自我迭代循环）
- 理论依据：Anthropic《Designing loops with Fable 5》五组件+记忆层（用户称 K3/fable5 原理）
- 落地五组件：
  1. 自动化触发：`automation_update` 创建 recurring automation `memoalign`，每日 09:00 触发（ACTIVE）
  2. 工作树隔离：`loop/worktree.ps1` 一键建隔离分支
  3. 技能文件：`.codebuddy/skills/memoalign-loop/SKILL.md`（项目约定/意图债务/迭代协议）
  4. 连接器：`_gh_push.py`（GitHub REST API，因本机 git push 常失败）
  5. 子代理：`Task`(code-explorer) 独立验证
  - 记忆层：`.codebuddy/memory/BACKLOG.md` + `MEMORY.md`
- 验证代理入口：`loop/verify.py`（编译+unittest，输出 tests/loop_verify.json，GBK 控制台安全打印）
- 文档：`docs/10_Loop工程化与自我迭代.md`
- 停止条件：P2 三项完成 + verify 全绿 + 文档同步 + 已推送

## 现状（verify）
- 上午（搭建 loop）：编译 PASS；test_pipeline 3 FAIL；test_runner 9 FAIL + 32 ERROR（均因测试代码未同步多记录抽取/缓存格式重构，非业务 bug）
- 下午（第一轮迭代完成）：**loop/verify.py 全绿**（compile PASS + test_pipeline PASS + test_runner PASS，passed=true）。44 项失败全部修复。

## 第一轮迭代执行记录（手动推进，v0.0.4）
- 用户说"想立刻推进" → 按 Loop 迭代协议执行：发现→规划→执行→验证→迭代→状态。
- 根因（5 类断言过时）：① extract() 现返回 List[Dict]（多记录）② store.put 缓存改 `{"__mtime":...,"records":[...]}` ③ `_rule_extract` 重命名为 `_rule_extract_section`（4 参数）④ `_first_heading` 现返回"标题 | 正文摘要"⑤ Graph 重构为 treemap/network（旧测试期望力导向 nodes/links/symbolSize）。
- 改动文件：tests/test_pipeline.py（TC04/05/07 取 [0]）、tests/test_runner.py（extract→[0]、st.put→st.put_all、_rule_extract_section 调用、build_graph_option mode="network"、put_slim 查 records[0]）、tests/test_data.json（EU01 期望 "Hi | c"、G04 数据 3+1 同语言不同组合）。
- 独立验证：派 code-explorer 子代理审查代码（确认逻辑一致、lint 0 错误），但其无命令执行能力；主代理补跑 `python loop/verify.py` 闭环 → ALL PASS。
- 收尾：清理诊断临时文件 → git commit → `_gh_push.py` 推送 master + 打 tag v0.0.4 → 更新 BACKLOG/MEMORY。

## 连接器与推送修复（v0.0.4 收尾）
- 原 `_gh_push.py` 是旧的 v0.0.3 硬编码脚本，首次推送把磁盘文件（含 v0.0.4 改动）推成 commit bf6e36a，但元数据错标 v0.0.3、漏推 Loop 产物、且混入 `.cache/`/`MemoAlign.log`(13MB)/`.pytest_cache`。
- 重写 `_gh_push.py`：版本参数、自动遍历仓库收集文件、排除日志/缓存、推送时**主动删除**远程冗余文件、tag 幂等（move）。
- 经多次清理推送，最终远程 master=4fb6cea，tag v0.0.4 落在该 commit，仓库干净（44 文件，无日志/缓存）。
- 注意：本机 git 与远程 API 推送为两条并行历史（本地 4f2565c vs 远程 4fb6cea，内容一致），后续迭代统一走 `_gh_push.py`(API) 以保持远程为权威源。
- 本地 `v0.0.4` tag 已存在（与远程 SHA 不同但内容同），无需强制移动。

## K3 讨论澄清（用户确认"K3=月之暗面新模型"）
- 用户最初把"K3/fable5"并列为自我迭代方法论；经联网检索确认：
  - **K3 = Kimi K3**：月之暗面 2026-07-16 发布的 2.8T 开源模型（MoE、KDA 混合注意力、100万token 上下文、原生视觉、面向软件工程/深度研究，全球最大开源模型）。是"模型/引擎"，非方法论。
  - **Fable 5 / Loop Engineering**：Anthropic 方法论（五组件+记忆层）。
- 本 Loop 架构**模型无关**：当前自动化默认 IDE 内置模型；未来可切 Kimi K3 API 或本地权重，五组件不动。
- 文档落地：`docs/10_Loop工程化与自我迭代.md` 新增 §2.1（K3 关系澄清）+ §7（每日 09:00 跑完一轮后的处理：成功路径 commit/push/tag/状态回写/停止判定；失败路径不推送+最多2轮修复+转人工）。
- 用户决定：暂不实际验证（不跑 loop 第一轮），仅把讨论写入文档。
